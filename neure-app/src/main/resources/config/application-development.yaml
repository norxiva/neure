spring:
  application:
    name: neureapp
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: root
#    url: jdbc:mysql://127.0.0.1:3306/neure?serverTimezone=GMT
    url: jdbc:mysql://127.0.0.1:3306/neure
mybatis:
  mapper-locations: classpath:mapper/*.xml
  config-location: classpath:mybatis-config.xml
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9010/eureka/
    fetch-registry: true

